import { View, Text } from 'react-native';

// Placeholder — implemented in Phase 6 (T-023)
export default function PostScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>İlan Ver — Phase 6</Text>
    </View>
  );
}
