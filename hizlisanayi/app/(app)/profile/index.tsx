import { View, Text } from 'react-native';

// Placeholder — implemented in Phase 9 (T-031)
export default function ProfileScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Profil — Phase 9</Text>
    </View>
  );
}
